# HTTP only — dùng khi build image (local / Docker không mount cert).
# Production + Let's Encrypt: xem nginx.ssl.scaify.nipio.conf và mount vào ui (DEPLOY_GCP.md Phần 6).

server {
    listen 80;
    server_name _;

    # Serve React static build
    root /usr/share/nginx/html;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Reverse proxy: /api → backend:8000
    location /api {
        proxy_pass         http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        client_max_body_size 50M;
    }

    # Reverse proxy: /rag → rag:8001 (SSE streaming)
    location /rag {
        proxy_pass          http://rag:8001;
        proxy_http_version  1.1;
        proxy_set_header    Connection        "";
        proxy_set_header    Host              $host;
        proxy_set_header    X-Real-IP         $remote_addr;
        proxy_set_header    X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header    X-Forwarded-Proto $scheme;
        proxy_buffering     off;
        proxy_cache         off;
        proxy_read_timeout  600s;
    }

    # SPA fallback — mọi route trả về index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
